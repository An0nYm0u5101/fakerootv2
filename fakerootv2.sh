clear
    
    echo "MEMULAI FAKEROOT..."
    echo "========== FAKEROOT BY Mr.PawN0 =========="
    echo "========== FAKEROOT AUTOSAVE    =========="
    echo
    echo
    echo
    apt install proot
    proot -0 -w ~ $PREFIX/bin/bash
    proot -0 -w ~ $PREFIX/bin/bash
    echo "SELESAI..."
    clear
